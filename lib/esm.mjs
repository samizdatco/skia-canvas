//
// Skia Canvas — ES Module version
//

import skia_canvas from './classes/index.js'

const {
  Canvas, CanvasGradient, CanvasPattern, CanvasRenderingContext2D, CanvasTexture,
  TextMetrics, Image, ImageData, Path2D, loadImage, FontLibrary, App, Window,
  DOMPoint, DOMMatrix, DOMRect
} = skia_canvas

export {
  Canvas, CanvasGradient, CanvasPattern, CanvasRenderingContext2D, CanvasTexture,
  TextMetrics, Image, ImageData, Path2D, loadImage, FontLibrary, App, Window,
  DOMPoint, DOMMatrix, DOMRect
}
